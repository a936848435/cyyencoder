flowchart TD
  %% 配色
  classDef gray fill:#F5F5F5,stroke:#9E9E9E,stroke-width:1.2px,stroke-dasharray:6 6;
  classDef orange fill:#FFE0B2,stroke:#EF6C00,stroke-width:1.8px,stroke-dasharray:6 4;
  classDef purple fill:#F3E5F5,stroke:#6A1B9A,stroke-width:1.8px,stroke-dasharray:6 4;
  classDef green fill:#DCEDC8,stroke:#388E3C,stroke-width:2.2px,stroke-dasharray:4 4;

  %% 1) INactive（简略）
  subgraph A["1) INactive 隐形触发器（简略）":::gray]
    A1[/输入：参考图像 + ROI 掩码/]
    A2[训练触发器生成器（U‑Net）\n目标：不可见且有效；记录同一 ROI 与归一化]
    A3[(输出：unet_trigger.pth)]
    A1 --> A2 --> A3
  end

  %% 2) CorruptEncoder：替换固定方块为隐形触发器（重点）
  subgraph B["2) 毒化样本生成 + MoCo 预训练（替换触发器）":::orange]
    direction TB
    B1[对象-背景接合（Gaussian 羽化）\nM_blur = GaussianBlur(M_obj, σ=1.0)\nI_comp = M_blur·I_obj + (1−M_blur)·I_bg]
    B2[隐形触发器注入（替代彩色方块）\nI_pois = clip(I_comp + λ·M_trig⊙U‑Net(I_comp | c_t, roi), 0,255)\n规则：对象↔触发器左右分区；trigger_marginal]
    B3[(导出：Poison 预训练集 + filelist/meta\n参数：area_ratio, object_marginal, support_ratio)]
  end

  %% 2) MoCo 预训练（流程不改）
  subgraph C["MoCo v2 编码器预训练（流程不改）":::purple]
    direction TB
    C1[/输入：毒化或混合数据；强增强两视角 x,x'/]
    C2[对比学习：f_q / f_k(EMA m=0.999)，队列 K=65536，温度 τ=0.2\nL = −log( exp(sim(z_q,z_k)/τ) / Σ exp(sim(z_q,z_i)/τ) )]
    C3[(输出：encoder ckpt)]
    C1 --> C2 --> C3
  end

  %% 3) 版权验证（多图令牌，黑盒）
  subgraph D["3) 版权验证：多图令牌（黑盒）":::green]
    direction TB
    D1[令牌生成 T：固定 seed 抽取 k 张 → 生成触发版\n保持与训练同 ROI/触发器与归一化]
    D2[黑盒查询：向可疑模型提交触发图\n记录目标类概率/命中次数 s]
    D3[统计判定：估计 p0（参考模型/1/C）\n门限 s* 控 FPR≤α；p‑value = P(X≥s|Bin(k,p0))]
    D4[(结论：p‑value<α → 归属成立；导出报告：样本/seed/阈值/日志)]
    D1 --> D2 --> D3 --> D4
  end

  %% 主线与引用
  A3 --> B2
  B1 --> B2 --> B3 --> C1
  C3 --> D1
  A3 -.同一触发器.-> D1